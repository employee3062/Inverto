{
  "name": "inverto",
  "version": "1.0.50",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts",
    "zip": "cd dist && zip handler.zip handler.js",
    "build": "bun build --minify-whitespace --minify-syntax --target bun --outfile dist/handler.js ./src/index.ts && bun run zip",
    "push": "bun run build && bun run push.ts",
    "deploy": "./deploy.sh",
    "destroy": "aws cloudformation delete-stack --stack-name inverto-stack"
  },
  "dependencies": {
    "@elysiajs/swagger": "^1.3.1",
    "elysia": "latest"
  },
  "devDependencies": {
    "@aws-sdk/client-lambda": "^3.891.0",
    "bun-types": "latest"
  },
  "module": "src/index.js"
}
